import React from 'react';
import ReactDOM from 'react-dom/client';
import { GifFlexApp } from './GifFlexApp';
import './style.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GifFlexApp />
  </React.StrictMode>,
);
